<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Profile | MYBLOGSPACE</title>
  <link rel="stylesheet" href="/profil.css" />
</head>
<body>
  <script>
  if (localStorage.getItem("siteTheme") === "light") {
    document.body.classList.add("light-mode");
  }
</script>
  <div class="sidebar">
    <h2>MYBLOGSPACE</h2>
    <nav>
      <a href="home">Home</a>
      <a href="notification">Notifications</a>
      <a href="messages">Messages</a>
      <a href="profil" class="active">Profile</a>
      <a href="login">Logout</a>
    </nav>
    <a href="create-blog">
    <button class="post-btn">Create Blog</button>
    </a>
  </div>

  <div class="main">
    <div class="cover-photo"></div>
    
    <div class="profile-section">
      <img src="<%= user.profilePic || '/images/default-pfp-23.jpg'%>" alt="Profile Picture" class="profile-pic" />
      <div class="profile-info">
        <h2><%= user.fullname %></h2>
<p class="username">@<%= user.fullname.replace(/\s/g, '') %></p>
<p class="bio"><%= user.bio || 'No bio yet.' %></p>
<p class="joined">Joined <%= user.joinedDate || 'June 2025' %></p>
      </div>
     <a href="/edit-profile"><button class="edit-btn">Edit Profile</button></a>
    </div>

    <div class="stats">
     
      <span><strong><%= user.followers.length %></strong> Followers</span>
     <span><strong><%= user.following.length %></strong> Following</span>
    </div>
    <div class="dashboard-button-container">
  <a href="/dashboard"><button class="dashboard-btn">Open Dashboard</button></a>
</div>

    <div class="posts">
    <h3>Your Posts</h3>
<% if (userBlogs.length === 0) { %>
  <p>No posts yet.</p>
<% } else { %>
  <% userBlogs.forEach(blog => { %>
    <div class="post">
      <h4><%= blog.title %></h4>
      <p>
  <%= blog.content.length > 200 ? blog.content.substring(0, 200) + '...' : blog.content %>
 <% if (blog.content.length > 200) { %>
    <a href="/blog/<%= blog._id %>">Read more</a>
  <% } %>
 </p>
      <span class="time"><%= blog.createdAt.toDateString() %></span>
     <a href="/edit-blog/<%= blog._id %>">
      <button class="edit-butn" style="margin-top: 10px;">Edit Post</button>
    </a>
    </div>
  <% }) %>
<% } %>

    </div>
  </div>
</body>
</html>
