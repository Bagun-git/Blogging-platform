<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= blog.title %> | MYBLOGSPACE</title>
  <link rel="stylesheet" href="/blog.css" />
</head>
<body>
  <div class="sidebar">
    <h2>MYBLOGSPACE</h2>
    <nav>
      <a href="/home">Home</a>
      <a href="/notification">Notifications</a>
      <a href="/messages">Messages</a>
      <a href="/profil">Profile</a>
      <a href="/login">Logout</a>
    </nav>
        <a href="create-blog">
    <button class="post-btn">Create Blog</button>
    </a>
  </div>

  <div class="main">
    <h1><%= blog.title %></h1>
    <p>By @<%= blog.authorName %> | <%= blog.createdAt.toDateString() %></p>
    <% if (blog.imagePath) { %>
      <img src="<%= blog.imagePath %>" alt="Blog Image" style="max-width: 55%; height: auto; display:block; margin: 15px auto;">
    <% } %>
   <%- blog.content
      .split('\n\n')
      .map(paragraph => `<p>${paragraph.replace(/\n/g, '<br>')}</p>`)
      .join('') %>
  </div>
</body>
</html>
